# joe richter
# Mar. 23, 2021
# Simplify a square root. In Python 57%10 is 7 (the remainder) and 57//10 is 5 (the quotient).
import math

sqrt = float(input("Input an unsimplified square root : "))
print(sqrt) 

if sqrt < 0: 
    print("Can't be a negative")
    exit(0)

